VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_APP_PUBLIC_URL=

# Server-only — used by api/public-employee-photo/[token].ts (a Vercel Edge
# Function). Never prefix these with VITE_, or they would be bundled into
# the client-side app and exposed to every visitor.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
